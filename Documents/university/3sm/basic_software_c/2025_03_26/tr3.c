#include <stdio.h>
#include <assert.h>

int main(int argc, char *argv[]){
  int num;
  int arr[10; 9; 8];

  printf("insira um número\n");
  scanf("%d", &num);
  for (int i = 0; i < 5; i++){
    {
    assert(num == arr[0]);
    }
  }
  return 0;
}
